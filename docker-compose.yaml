version: "3.9"

services:
  mysql:
    image: mysql:8.0.32
    environment:
      - MYSQL_ROOT_PASSWORD=rootroot
      - MYSQL_DATABASE=spingbootdb
      - MYSQL_USER=wen
      - MYSQL_PASSWORD=12345
    expose:
      - 3306
    ports:
      - "3306:3306"
    networks:
      - default

networks:
  default: